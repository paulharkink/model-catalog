apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: vllm-eugr-build
  namespace: ci
  annotations:
    description: |
      Manually-triggered pipeline to build and push a vllm-eugr base image.
      Image label: docker.internal.phis.nl/vllm-eugr:<vllm_ref> (e.g. vllm-eugr:0.19.0)
      Idempotent: skips build if image already exists in nexus.
spec:
  params:
    - name: vllm_ref
      type: string
      description: git ref to check out from eugr/spark-vllm-docker (e.g. v0.19.0)
  workspaces:
    - name: eugr
  results:
    - name: image
      description: vllm-eugr image label (always set, even when skipped)
      value: $(tasks.check-and-prepare.results.image)
  tasks:
    - name: check-and-prepare
      taskSpec:
        params:
          - name: vllm_ref
            type: string
        workspaces:
          - name: eugr
        volumes:
          - name: github-app
            secret:
              secretName: ci-github-app
          - name: github-helper
            configMap:
              name: github-app-helper
              defaultMode: 0755
          - name: docker-config
            secret:
              secretName: docker-internal-registry
              items:
                - key: .dockerconfigjson
                  path: config.json
        results:
          - name: image
            description: Computed vllm-eugr image label
          - name: skip
            description: "'true' if image already exists in nexus, 'false' if build is needed"
        steps:
          - name: check
            image: alpine/git:2.45.2
            computeResources:
              requests: { cpu: "250m", memory: "256Mi" }
              limits: { cpu: "1", memory: "1Gi" }
            volumeMounts:
              - name: github-app
                mountPath: /var/secrets/github-app
              - name: github-helper
                mountPath: /helpers
              - name: docker-config
                mountPath: /docker-config
            script: |
              set -eux
              apk add --no-cache openssl curl jq python3 >/dev/null 2>&1
              REF=$(printf '%s' "$(params.vllm_ref)" | sed 's/^v//')
              IMAGE="docker.internal.phis.nl/vllm-eugr:${REF}"
              printf '%s' "${IMAGE}" > /tekton/results/image
              mkdir -p "$(workspaces.eugr.path)/meta"
              printf '%s\n' "${IMAGE}" > "$(workspaces.eugr.path)/meta/image"

              AUTH=$(python3 -c "import json; d=json.load(open('/docker-config/config.json')); print(d['auths']['docker.internal.phis.nl']['auth'])" 2>/dev/null || echo "")
              STATUS="000"
              if [ -n "${AUTH}" ]; then
                STATUS=$(curl -sf -o /dev/null -w "%{http_code}" \
                  -H "Authorization: Basic ${AUTH}" \
                  -H "Accept: application/vnd.docker.distribution.manifest.v2+json" \
                  "https://docker.internal.phis.nl/v2/vllm-eugr/manifests/${REF}" 2>/dev/null || echo "000")
              fi

              if [ "${STATUS}" = "200" ]; then
                echo "Image ${IMAGE} already exists in nexus, skipping build."
                printf 'true' > /tekton/results/skip
                printf 'true\n' > "$(workspaces.eugr.path)/meta/skip"
                exit 0
              fi

              printf 'false' > /tekton/results/skip
              printf 'false\n' > "$(workspaces.eugr.path)/meta/skip"

              TOKEN=$(/helpers/github-app-token.sh /var/secrets/github-app)
              rm -rf "$(workspaces.eugr.path)/src"
              git clone "https://x-access-token:${TOKEN}@github.com/eugr/spark-vllm-docker.git" \
                "$(workspaces.eugr.path)/src"
              cd "$(workspaces.eugr.path)/src"
              git checkout "$(params.vllm_ref)"
      params:
        - name: vllm_ref
          value: $(params.vllm_ref)
      workspaces:
        - name: eugr
          workspace: eugr

    - name: kaniko-build
      when:
        - input: $(tasks.check-and-prepare.results.skip)
          operator: in
          values: ["false"]
      runAfter: [check-and-prepare]
      taskSpec:
        workspaces:
          - name: eugr
        volumes:
          - name: docker-config
            secret:
              secretName: docker-internal-registry
              items:
                - key: .dockerconfigjson
                  path: config.json
        steps:
          - name: build
            image: gcr.io/kaniko-project/executor:debug
            computeResources:
              requests: { cpu: "4", memory: "16Gi" }
              limits: { cpu: "8", memory: "32Gi" }
            timeout: 5400s
            env:
              - name: DOCKER_CONFIG
                value: /kaniko/.docker
            volumeMounts:
              - name: docker-config
                mountPath: /kaniko/.docker
            command: ["/busybox/sh", "-c"]
            args:
              - |
                set -eu
                read -r IMAGE < "$(workspaces.eugr.path)/meta/image"
                /kaniko/executor \
                  --dockerfile="$(workspaces.eugr.path)/src/Dockerfile" \
                  --context="$(workspaces.eugr.path)/src" \
                  --destination="${IMAGE}" \
                  --skip-tls-verify \
                  --cache=false
      workspaces:
        - name: eugr
          workspace: eugr
---
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  generateName: vllm-eugr-build-
  namespace: ci
  annotations:
    description: |
      Manual trigger for building a vllm-eugr base image.
      Run: kubectl create -f this-file (generateName creates a new PipelineRun each time).
spec:
  pipelineRef:
    name: vllm-eugr-build
  taskRunTemplate:
    serviceAccountName: tekton-runner
    podTemplate:
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/gpu-worker
                    operator: Exists
  params:
    - name: vllm_ref
      value: "v0.19.0"
  workspaces:
    - name: eugr
      emptyDir: {}
